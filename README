Install Java, Simple Build Tool, and Git

Clone this repository with:
git clone git://pc575-ubu/docreg-web.git

Then cd into docreg-web and type:
sbt

At the sbt prompt, type:
update

Then:
jetty-run

Point your browser to:
http://localhost:9333/

Voila a running docreg+web app
